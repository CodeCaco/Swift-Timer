import SwiftUI

extension Color {
    static let appBackground = Color("DeepNavy")
    static let appPrimaryText = Color("Ivory")
    static let appSecondaryText = Color("Ivory").opacity(0.75)
    static let appAccent = Color("AppTeal")
    static let appTrack = Color("Ivory").opacity(0.25)
}
